import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'binary1702.winamp',
  appName: 'Winamp',
  webDir: 'out',
  ios: {
    contentInset: 'never',
    backgroundColor: '#0d0f12',
  },
};

export default config;
